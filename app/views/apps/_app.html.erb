<li class="install<%= last %>">
  <%= icon(app) %>
  <div class="heading">
    <%= heading(app) %>
    <%= stars(app.average_rating) %>
    <%= buy_link(app) %>
  </div>
  <div class="details">
    <p>
      updated by <%= pluralize(app.number_of_installs, 'user') %>
    </p>
  </div>
</li>
